<!DOCTYPE html>
<html>
    <head>
        <title>GarbageCollection - {{viewName}}</title>
    </head>
    <body>
        <h1>{{viewName}} View</h1>

        <nav>
            <a href="/home">Home</a> |
            <a href="/app">App</a> |
            <a href="/complainment">Complanment</a> |
        </nav>
        <br>

        {% if viewName == "Home" %}
            GarbageCollector v1.3.3.7
        {% endif %}

        {% if viewName == "Complainment" %}
            <form action="complainment/submit" method="post" id="comp-form">
                {% for type in types %}
                    <input type="radio" name="complainment" value="{{type}}"> {{type}}<br>
                {% endfor %}
                <textarea type="text" name="description" cols="100" rows="5" placeholder="Description goes here..."></textarea><br>
            </form>
            <button type="submit" form="comp-form" value="Submit">Submit</button>
        {% endif %}

        {% if viewName == "App" %}
            App Interactions
        {% endif %}
    </body>
</html>
